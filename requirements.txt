# Core dependencies
torch
torchvision
transformers
datasets
accelerate

# Vision and OCR
# Both OCR backends installed: DeepSeek (primary) with PaddleOCR (fallback)
# If DeepSeek-OCR fails due to OOM, code automatically falls back to PaddleOCR
deep-ocr                      # Primary: Heavy OCR backend (~10GB VRAM, high accuracy)
paddleocr>=2.7.0              # Fallback: Lightweight OCR backend (~200MB VRAM)
paddlepaddle-gpu>=2.5.0       # GPU support for PaddleOCR
ultralytics
opencv-python
Pillow

# LLM and NLP
sentencepiece
protobuf
tokenizers

# Image processing
imagehash
nsfw-image-detector

# CLIP and vision models
open-clip-torch
timm

# Utilities
numpy
pandas
scikit-learn
matplotlib
seaborn
tqdm
pyyaml
jsonlines

# Evaluation
pycocotools
rapidfuzz

# Development
pytest
pytest-cov
black
flake8
mypy

# Optional: For limited GPU resources
peft
bitsandbytes

# Jupyter support
jupyter
ipywidgets
ipykernel
